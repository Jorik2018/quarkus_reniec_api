package gob.regionancash;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeRENIECResourceIT extends ResourceTest {

    // Execute the same tests but in native mode.
}